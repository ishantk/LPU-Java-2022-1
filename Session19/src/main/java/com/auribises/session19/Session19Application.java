package com.auribises.session19;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Session19Application {

	public static void main(String[] args) {
		SpringApplication.run(Session19Application.class, args);
	}

}

// Assignment: Create a WebService in SpringBoot which returns data of Students in a Batch